name: Temporary VPS (Ngrok SSH + Countdown)

on:
  workflow_dispatch:
    inputs:
      ssh_user:
        description: '👤 SSH username'
        required: true
        default: 'runner'
      ssh_password:
        description: '🔑 SSH password'
        required: true
        default: 'MySecurePass123'
      max_runtime:
        description: '⏱️ Max runtime (minutes, max 350)'
        required: true
        default: '330'
        type: choice
        options:
          - '60'
          - '120'
          - '180'
          - '240'
          - '300'
          - '330'
          - '350'

jobs:
  vps:
    runs-on: ubuntu-24.04
    timeout-minutes: 350

    steps:
      - name: Check runtime
        id: runtime
        run: |
          MAX="${{ github.event.inputs.max_runtime }}"
          if [ "$MAX" -gt 350 ]; then MAX=350; fi
          echo "max=$MAX" >> $GITHUB_OUTPUT

      - name: Setup SSH + Ngrok tunnel + Countdown
        env:
          SSH_USER: ${{ github.event.inputs.ssh_user }}
          SSH_PASS: ${{ github.event.inputs.ssh_password }}
          MAX_MINUTES: ${{ steps.runtime.outputs.max }}
          NGROK_TOKEN: ${{ vars.NGROK_AUTH_TOKEN }}
        run: |
          set -e
          echo ">>> Cài đặt công cụ..."
          sudo apt update -qq && sudo apt install -y -qq openssh-server curl jq tmux

          echo ">>> Tạo/cập nhật user $SSH_USER..."
          if id "$SSH_USER" &>/dev/null; then
            echo "$SSH_USER:$SSH_PASS" | sudo chpasswd
          else
            sudo useradd -m -s /bin/bash "$SSH_USER"
            echo "$SSH_USER:$SSH_PASS" | sudo chpasswd
            sudo usermod -aG sudo "$SSH_USER"
          fi

          echo ">>> Cấu hình SSH cho phép đăng nhập mật khẩu..."
          sudo sed -i 's/^#PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i 's/^PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart ssh

          echo ">>> Cài đặt Ngrok..."
          curl -sSL https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com bookworm main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update -qq && sudo apt install -y -qq ngrok

          echo ">>> Xác thực Ngrok..."
          ngrok config add-authtoken "$NGROK_TOKEN"

          echo ">>> Mở tunnel TCP 22..."
          nohup ngrok tcp 22 --log=stdout > /tmp/ngrok.log 2>&1 &
          sleep 5

          NGROK_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url' | sed 's/tcp:\/\///')
          if [ -z "$NGROK_URL" ]; then
            echo "❌ Không lấy được tunnel. Log:"
            cat /tmp/ngrok.log
            exit 1
          fi

          SSH_HOST=$(echo "$NGROK_URL" | cut -d: -f1)
          SSH_PORT=$(echo "$NGROK_URL" | cut -d: -f2)

          # Tạo script countdown.sh
          cat > /home/runner/countdown.sh << 'SCRIPT'
          #!/bin/bash
          MAX_MINUTES_PLACEHOLDER
          END_TIME=$(( $(date +%s) + MAX_MINUTES_PLACEHOLDER * 60 ))
          while true; do
            NOW=$(date +%s)
            REMAINING=$(( END_TIME - NOW ))
            if [ $REMAINING -le 0 ]; then
              echo "Hết giờ! VPS sẽ tắt."
              break
            fi
            clear
            echo "========================================"
            echo "  VPS TEMPORARY - ĐẾM NGƯỢC THỜI GIAN"
            echo "  Thời gian còn lại: $(( REMAINING / 60 )) phút $(( REMAINING % 60 )) giây"
            echo "========================================"
            echo ""
            echo "  👉 Nhấn Ctrl+B sau đó nhấn D để thoát"
            echo "     (không dùng lệnh exit)"
            echo ""
            sleep 30
          done
          SCRIPT

          # Thay thế biến thời gian
          sed -i "s/MAX_MINUTES_PLACEHOLDER/$MAX_MINUTES/g" /home/runner/countdown.sh
          chmod +x /home/runner/countdown.sh

          # Khởi động tmux session tên "thoigian"
          tmux new-session -d -s thoigian "/home/runner/countdown.sh"

          echo "=============================================="
          echo "✅ VPS đã sẵn sàng!"
          echo ""
          echo "💻 SSH command:"
          echo "   ssh -p $SSH_PORT $SSH_USER@$SSH_HOST"
          echo "🔑 Password: $SSH_PASS"
          echo ""
          echo "📱 Dùng app (Termius, JuiceSSH...):"
          echo "   Host: $SSH_HOST"
          echo "   Port: $SSH_PORT"
          echo "   User: $SSH_USER"
          echo "   Pass: $SSH_PASS"
          echo ""
          echo "⏱️  Tự hủy sau: $MAX_MINUTES phút"
          echo "🕒 Đồng hồ đếm ngược (trong terminal):"
          echo "   tmux attach -t thoigian"
          echo "   (Nhấn Ctrl+B, D để thoát)"
          echo "=============================================="

          # Bộ đếm trên Action log
          for ((i=MAX_MINUTES; i>0; i--)); do
            echo "⏱️  Thời gian còn lại: $i phút"
            sleep 60
          done
          echo "🛑 Hết giờ, VPS dừng."
        shell: bash
